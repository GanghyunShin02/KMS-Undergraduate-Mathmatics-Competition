# 전국대학생수학경시대회 제2분야 문제 (2003-2025)

> 풀이와 답안은 제외하고 문제만 수록하였다. 2005년 자료는 제공된 제2차(오후) 문제만 포함하며, 2022년 자료는 제공되지 않아 수록하지 않았다.

## 2003년 제22회

### 제1차 (오전)

#### 1.

다음 값을 구하여라.

$$ \sum_{k=1}^{\infty}\binom{10^{10}}{\left\lfloor \log_{11}k\right\rfloor} $$

단, $\binom{n}{r}$은 이항계수 ${}_nC_r$을 의미하며, $\lfloor x\rfloor$는 $x$를 넘지 않는 최대 정수를 의미한다.

sol.

$N=10^{10}$ 이라고하면

1~9 
$\binom{10^{10}}{0}$

10~99 
$\binom{10^{10}}{1}$

$10^N ~ 10^{N+1} -1 \binom{10^N}{10^N}$

일반화하면 

$$
\sum_{n=0}^N (10^{n+1}-1-10^n) \binom{N}{n}
$$

$$
\sum_{n=0}^N 9 \cdot 10^n \binom{N}{n}-\sum_{n=0}^N \binom{N}{n}
$$

이항정리에 의해 $9 \cdot 11^N-2^N$

---

#### 2.

네 개의 점 $A,B,C,D$가 고정된 원의 원주 위에 시계 방향으로 주어져 사각형 $ABCD$가 원의 중심을 포함한다고 한다. 네 개의 삼각형 $ABC$, $BCD$, $CDA$, $DAB$의 내심을 각각 $P,Q,R,S$라고 할 때, 사각형 $PQRS$는 직사각형임을 증명하여라.

#### 3.

자연수 $a,b$, 그리고 소수 $p,q$에 대하여

$$ p^a-q^b=1 $$

을 만족시키는 자연수의 순서쌍 $(p^a,q^b)$을 모두 구하여라. 단, $p<2003$이고 $q<2003$이다.

sol.

2를 제외한 소수의 거듭제곱은 항상 홀수이다. $p^a=q^b+1$ 에서 $p^a,q^b$ 둘 중하나는 짝수여야 하므로 
p혹은 q는 2이다.

case 1. q=2
$p^a=2^b+1$

a=1 : $p=2^b+1$. 이를 만족하는 순서쌍은 (3,2),(5,4),(17,16),(257,256).
$a\ne 1$ : a가 홀수이면 $2^b=p^a-1=(p-1)(p^{a-1}+p^{a-2}+...+1)$ 로 인수분해가능
p의 거듭제곱은 홀수인데 $(p^{a-1}+p^{a-2}+...+1)$에서 이를 홀수번 더하므로 이 항또한 홀수.
$2^b$ 가 홀수인 인수를 가지게되므로 불가능.

a가 짝수이면 $p^a-1=(p^{a-1}-1)(p^{a+1}+1)$로 인수분해가능하고 이를 만족하는 순서쌍은 (9,8).

case 2. p=2
$2^a=p^b+1$

b가 짝수이면:
$2^a=p^{2k}+1$
모듈러.. $x\equiv n (mod m)$ 이라는 뜻은 x를 m으로 나눈 나머지가 n이라는거다.
$p^2\equiv 1 (mod 8)$ 이 모든 홀수 p에대해 성립함..
$p^{2k}\equiv 1 (mod 8)$ (거듭제곱해도 성립) 이므로 $2^a-1$ 역시 이를 만족해야하지만 그렇지않음.

b가 홀수이면:
$2^a=(q+1)(q^{b-1}+q^{b-2}+...+1) 도 q=2의 경우에서와 같이 성립하지않음.

따라서 p=2이면 b=1이여야 조건이 성립. 그 순서쌍은 (4,3),(8,7),(32,31),(128,127).

---



#### 4.

수열 $\lbrace x_n\rbrace$은 모든 자연수 $n$에 대하여

$$ 0\le x_{n+1}\le x_n $$

을 만족시키며, 급수

$$ \sum_{n=1}^{\infty}x_n $$

이 수렴한다고 가정하자. 이때 다음 극한값을 구하여라.

$$ \lim_{n\to\infty}nx_n $$

sol.

$$
\sum_{n=1}^{\infty} x_n
$$

이 수렴하므로

$$
\lim_{m\to\infty}\sum_{k=m}^{\infty}x_k=0.
$$

또한 $\{x_n\}$은 감소수열이므로

$$
x_k \ge x_n
\qquad
\left(
\left\lfloor \frac{n}{2} \right\rfloor+1
\le k \le n
\right).
$$

따라서

$$
\sum_{k=\left\lfloor n/2 \right\rfloor+1}^{n}x_k
\ge
\frac{n}{2}x_n.
$$

그러므로

$$
0\le nx_n
\le
2\sum_{k=\left\lfloor n/2 \right\rfloor+1}^{n}x_k
\le
2\sum_{k=\left\lfloor n/2 \right\rfloor+1}^{\infty}x_k.
$$

그런데

$$
\lim_{n\to\infty}
2\sum_{k=\left\lfloor n/2 \right\rfloor+1}^{\infty}x_k
=0.
$$

따라서 샌드위치 정리에 의해

$$
\boxed{\lim_{n\to\infty} nx_n=0}.
$$

---

#### 5.

$n$차원 벡터 $\mathbf{x}=(x_1,x_2,\ldots,x_n)\in\mathbb{R}^n$가

$$ x_1+x_2+\cdots+x_n=0 $$

을 만족시킨다고 한다. 다음 부등식이 성립함을 증명하여라.

$$ \max_{1\le j\le n}|x_j| \le \left(\frac{n-1}{n}\right)^{1/2}\lVert\mathbf{x}\rVert_2 $$

단,

$$ \lVert\mathbf{x}\rVert_2 = \left(x_1^2+x_2^2+\cdots+x_n^2\right)^{1/2} $$

이다.

sol.

$\max_{1\le j\le n}|x_j|=x_k$ 
라고하자.

주어진 부등식의 양변을 제곱하면

$$
|x_{max} |^2 \le \frac{n-1}{n} (x_1 ^2+x_2 ^2 +...+ x_n ^2)
$$

정리하면 

$$
|x_{max} |^2 -\frac{n-1}{n} x_{max} ^2 \le \frac{n-1}{n} \sum_{i\neq k} ^n x_i ^2
$$

x의 부호와 상관없이 $|x|^2=x^2$ 이므로

$$
\frac{1}{n} x_k ^2 \le \frac{n-1}{n} \sum_{i\neq k}x_i ^2= \sum_{i \neq k} ^n x_i ^2 -\frac{1}{n} \sum_{i \neq k} ^n x_i^2
$$

$$
\frac{1}{n} \sum x_i ^2 \le \sum_{i \neq k} ^n x_i^2
$$

이 되므로 주어진 부등식은 성립한다.



$\max_{1\le j\le n}|x_j|=|x_k|$라고 하자.

주어진 조건

$$
x_1+x_2+\cdots+x_n=0
$$

에 의해

$$
x_k=-\sum_{i\ne k}x_i.
$$

따라서

$$
x_k^2=
\left(\sum_{i\ne k}x_i\right)^2.
$$

코시-슈바르츠 부등식에 의해

$$
\left(\sum_{i\ne k}x_i\right)^2=
\left(\sum_{i\ne k}x_i\cdot 1\right)^2
\le
\left(\sum_{i\ne k}x_i^2\right)
\left(\sum_{i\ne k}1^2\right).
$$

$i\ne k$인 항은 총 $n-1$개이므로

$$
\sum_{i\ne k}1^2=n-1.
$$

따라서

$$
x_k^2
\le
(n-1)\sum_{i\ne k}x_i^2.
$$

양변에 $(n-1)x_k^2$을 더하면

$$
nx_k^2
\le
(n-1)\sum_{i=1}^{n}x_i^2.
$$

따라서

$$
x_k^2
\le
\frac{n-1}{n}\sum_{i=1}^{n}x_i^2.
$$

즉,

$$
|x_k|^2
\le
\frac{n-1}{n}\lVert\mathbf{x}\rVert_2^2.
$$

양변에 제곱근을 취하면

$$
|x_k|
\le
\left(\frac{n-1}{n}\right)^{1/2}
\lVert\mathbf{x}\rVert_2.
$$

그러므로

$$
\boxed{
\max_{1\le j\le n}|x_j|
\le
\left(\frac{n-1}{n}\right)^{1/2}
\lVert\mathbf{x}\rVert_2
}
$$

가 성립한다.

---


### 제2차 (오후)

#### 6.

자연수 $k=1,2,\ldots,2002$에 대하여 $k-1\le x_k\le k$를 만족시키는 $2002$개의 실수 $x_1,x_2,\ldots,x_{2002}$가 주어졌다고 가정하자. 이때 집합 $B_k$와 $A$를

$$ B_k=\lbrace \frac{2}{k}\sum_{i=1}^{k}|x-x_i| \mid x\in\mathbb{R}\rbrace $$

및

$$ A=\lbrace \sum_{k=1}^{2002}y_k \mid y_k\in B_k,\ k=1,2,\ldots,2002\rbrace $$

와 같이 정의하였을 때, 다음 이항계수는 집합 $A$의 원소임을 증명하여라.

$$ \binom{2003}{2} $$

#### 7.

실수 $\sin 1$이 무리수임을 증명하여라.

#### 8.

함수 $f:[0,3]\to\mathbb{R}$은 폐구간 $[0,3]$ 위에 정의된 연속함수이며, 정수 $k=0,1,2,\ldots,n-1$에 대하여

$$ \int_0^3x^kf(x)\,dx=0 $$

과

$$ \int_0^3x^nf(x)\,dx=3 $$

을 만족시킨다고 한다. 그러면 구간 $A$에 속한 모든 $x\in A$에 대하여

$$ |f(x)|\ge\left(\frac{2}{3}\right)^n(n+1) $$

을 만족시키는 구간 $A\subset[0,3]$가 존재함을 증명하여라.

#### 9.

함수 $f$를 다음과 같이 정의한다.

$$ f(x)=\pi+x \qquad (-\pi<x\le0) $$

$$ f(x)=\pi-x \qquad (0<x\le\pi) $$

그리고 $f(x+2\pi)=f(x)$가 되도록 주기적으로 연장하자. 이 함수의 Fourier 급수를 이용하여

$$ \frac{\pi^2}{8} = \frac{1}{1^2}+\frac{1}{3^2}+\cdots+\frac{1}{(2n-1)^2}+\cdots $$

임을 증명하여라.

---

## 2004년 제23회

### 제1차 (오전)

#### 1.

함수 $f:\mathbb{R}\to\mathbb{R}$가 모든 실수 $x,y\in\mathbb{R}$에 대하여

$$ f(x+y)=f(x)+f(y) $$

를 만족시킨다고 할 때, 다음이 성립함을 보여라.

1. 함수 $f$가 $x=0$에서 연속이면, 함수 $f$는 모든 점 $x\in\mathbb{R}$에서 연속이다.
2. 함수 $f$가 $\mathbb{R}$에서 연속이고 $f(1)=c$라면, 모든 $x\in\mathbb{R}$에 대하여 $f(x)=cx$이다.

#### 2.

밑면과 윗면이 서로 평행하고 밀도가 고른 각뿔대의 질량중심(무게중심)으로부터 밑면까지의 거리와 윗면까지의 거리의 비를 구하여라. 단, 밑면에 대한 윗면의 닮음비는 $r$ $(r<1)$이다.

#### 3.

각 변의 길이가 변 $AB$로부터 순서대로 $a,b,c,d$로 주어진 볼록사각형 $ABCD$의 면적이 최대가 되기 위해서는 그 사각형이 원에 내접해야 함을 보여라.

#### 4.

$\lbrace\mathbf{v}_1,\mathbf{v}_2,\ldots,\mathbf{v}_n\rbrace$을 $n$차원 벡터공간 $\mathbb{R}^n$의 정규직교 기저라고 하자. 다음 벡터들이 생성하는 부분공간의 직교 여공간(orthogonal complement)을 구하여라.

$$ i\mathbf{v}_i-j\mathbf{v}_j \qquad (1\le i<j\le n) $$

#### 5.

함수 $f:[a,c]\to(0,\infty)$가 구간 $[a,c]$에서 연속이고 구간 $(a,c)$에서 미분가능하다. 함수 $f$의 그래프 위의 점 $(x_0,f(x_0))$을 $P$라고 하자. 점 $P$와 점 $(a,0)$, 점 $P$와 점 $(c,0)$을 잇는 각 직선이 $x$축과 이루는 각을 각각 $\alpha$와 $\gamma$라고 하고, 점 $P$에서의 접선이 $x$축과 이루는 각을 $\beta$라고 할 때,

$$ \tan\alpha+\tan\beta+\tan\gamma=0 $$

을 만족시키는 $x_0$가 구간 $(a,c)$ 내에 존재함을 보여라. 단, 접선이 $x$축과 만나지 않는 경우에는 $\beta=0$으로 한다.

### 제2차 (오후)

#### 6.

함수 $f:\mathbb{R}\to\mathbb{R}$의 2계 도함수 $f''(x)$가 구간 $[0,1]$에서 존재하고 유계이다. 모든 자연수 $n$에 대하여 $0<a_n<1$인 수열 $\lbrace a_n\rbrace$의 급수 $\sum_{n=1}^{\infty}a_n$은 발산하지만 $\sum_{n=1}^{\infty}a_n^2$은 수렴한다고 할 때, 급수 $\sum_{n=1}^{\infty}f(a_n)$이 수렴하면 급수 $\sum_{n=1}^{\infty}|f(a_n)|$도 수렴함을 보여라.

#### 7.

좌표평면에서 $T$를 주기로 갖는 곡선 $(x(t),y(t))$가 양수 $a,b,c,d$에 대하여 미분방정식

$$ \frac{dx}{dt}=ax-bxy, \qquad \frac{dy}{dt}=-cy+dxy, \qquad x(0)=1, \qquad y(0)=1 $$

을 만족시킨다고 할 때, 다음 두 값을 구하여라.

$$ \overline{x}:=\frac{1}{T}\int_0^T x(t)\,dt, \qquad \overline{y}:=\frac{1}{T}\int_0^T y(t)\,dt $$

#### 8.

다음 적분을 계산하여라.

$$ \int_0^\pi\frac{x\sin x}{1+\cos^2x}\,dx $$

#### 9.

집합 $\lbrace1,2,\ldots,39,40\rbrace$의 크기가 $|B|=30$인 임의의 부분집합 $B$에 대하여, 다음 조건을 만족시키는 $B$의 부분집합 $A$가 존재함을 보여라.

1. $|A|\ge10$.
2. 임의의 원소 $x,y\in A$에 대하여 $x+y\notin A$.

---

## 2005년 제24회

### 제2차 (오후)

#### 6.

담배를 피우는 사람과 피우지 않는 사람으로 구성된 집단에서, 매년 담배를 피우지 않는 사람의 $5\%$가 담배를 피우고 담배를 피우는 사람의 $10\%$가 담배를 끊는다면, 아주 오랜 시간이 흐른 후 담배를 피우는 사람과 피우지 않는 사람의 비율은 일정한 값에 수렴하게 됨을 보이고 그 비율을 구하라.

#### 7.

역행렬을 갖는 $2\times2$ 행렬 전체의 집합을 $X$라 하고, 역행렬을 갖는 $3\times3$ 행렬 전체의 집합을 $Y$라 하자. 함수 $\Phi:X\to Y$를

$$ \Phi\left(\begin{bmatrix}a&b\cr c&d\end{bmatrix}\right) = \begin{bmatrix} a^2&2ab&b^2\cr ac&ad+bc&bd\cr c^2&2cd&d^2 \end{bmatrix} $$

로 정의하자. 행렬 $A\in X$는 $1,x$를 고유값으로 갖고, 행렬 $B\in X$는 $1+x,1-x$를 고유값으로 갖는다고 가정할 때 다음 값을 구하라.

$$ \det[\Phi(AB)] $$

#### 8.

세 문자 “가, 나, 다”를 사용하여 길이가 $n$인 단어를 만든다고 하자. 단, 문자 “가”를 두 번 연속하여 사용할 수 없다. 이때 만들 수 있는 길이가 $n$인 단어의 개수를 $n$에 관한 식으로 나타내라.

#### 9.

두 번 미분 가능한 함수 $f:(0,\infty)\to\mathbb{R}$가 모든 $x\in(0,\infty)$에 대하여

$$ |f(x)|\le1, \qquad |f''(x)|\le1 $$

을 만족할 때, 모든 $x\in(0,\infty)$에 대하여 $|f'(x)|\le2$임을 보여라.

---

## 2006년 제25회

### 제1차 문제 (제1·제2분야 공통)

#### 1.

함수 $f(x)=\arctan(2x)$에 대하여 $f^{(99)}(0)$의 값을 구하여라.

#### 2.

함수 $f:\mathbb{R}\to\mathbb{R}$은 주기가 $2T$인 연속함수이다. 이때 적당한 $x_0$에 대하여

$$ f(x_0+T)=f(x_0) $$

가 성립함을 보여라.

#### 3.

임의의 $n\times n$ 행렬 $X,Y$에 대하여

$$ \mathrm{trace}(XYA)=\mathrm{trace}(YXA) $$

를 만족시키는 $n\times n$ 행렬 $A$는 단위행렬(항등행렬)의 상수배임을 보여라. 여기에서 trace는 대각 원소의 합을 나타낸다.

#### 4.

양수들의 수열 $a_n$에 대하여 극한값

$$ \lim_{n\to\infty}n\left(\frac{a_n}{a_{n+1}}-1\right) $$

이 존재한다. 이 극한값이 1보다 크면 급수

$$ \sum_{n=1}^{\infty}a_n $$

이 수렴함을 보여라.

#### 5.

함수 $f:(0,\infty)\to\mathbb{R}$의 도함수 $f'$가 연속이고

$$ \lim_{x\to\infty}\bigl(f(x)+f'(x)\bigr)=0 $$

이면

$$ \lim_{x\to\infty}f(x)=0 $$

임을 보여라.

### 제2차 문제 (제2분야)

#### 6.

다음 적분을 계산하여라.

$$ \int_{-\pi}^{\pi}\ln\left|\sin\frac{\theta}{2}\right|\,d\theta $$

#### 7.

단위벡터 $(a_1,a_2,\ldots,a_n)$에 대하여 $n\times n$ 행렬 $X=(x_{ij})$를

$$ x_{ij}=\delta_{ij}+a_ia_j $$

로 정의할 때, $X$의 행렬식을 계산하여라. 여기에서 $\delta_{ij}$는 $i=j$일 때 1이고 $i\ne j$일 때 0이다.

#### 8.

함수 $h:\mathbb{R}\to\mathbb{R}$에 대하여

$$ \int_0^{\infty}h(t)\,dt=1 $$

일 때, 다음 적분을 계산하여라.

$$ \iint_{\mathbb{R}^2}h(x^2+2xy+5y^2)\,dA $$

#### 9.

다음 급수를 계산하여라.

$$ 1-\frac15+\frac17-\frac1{11}+\cdots+ \frac1{6n-5}-\frac1{6n-1}+\cdots $$

---

## 2007년 제1회 전국 대학생 공학수학 경시대회

### 제1차 문제

#### 1-1.

다음 적분을 계산하여라.

$$ \int_0^1\int_0^x(3-x-y)\,dy\,dx $$

#### 1-2.

곡선 $C(t)=(\cos t,\sin t)$, $0\le t\le2\pi$에 대하여 다음 선적분을 계산하여라.

$$ \int_C\frac{y\,dx-x\,dy}{x^2+y^2} $$

#### 1-3.

꼭지점의 좌표가 각각 $(0,0,0)$, $(1,2,3)$, $(3,1,2)$, $(7,4,7)$인 사각형의 넓이를 계산하여라.

#### 1-4.

함수

$$ f(x)=\frac{e^x}{x}, \qquad 1\le x\le2 $$

의 역함수 $g$에 대하여 다음 적분을 계산하여라.

$$ \int_e^{e^2/2}[g(x)]^2\,dx $$

#### 1-5.

다음 방정식을 만족시키는 함수 $x(t),y(t)$를 구하여라.

$$ x'(t)=-y(t), \qquad y'(t)=x(t), \qquad (x(0),y(0))=(1,1) $$

#### 1-6.

연립방정식

$$ \begin{aligned} x_1-2x_2-x_3+3x_4&=a,\cr 2x_1+4x_2+6x_3-2x_4&=b,\cr x_1+x_3+x_4&=c \end{aligned} $$

가 해를 갖기 위해 $a,b,c$가 만족시켜야 하는 관계식을 구하여라.

#### 1-7.

행렬

$$ \begin{bmatrix} 1&-3&0\cr 0&1&3\cr 2&-10&2 \end{bmatrix} $$

을 하삼각행렬 $L$과 상삼각행렬 $U$의 곱 $LU$로 써라.

#### 1-8.

3차원 공간 $\mathbb{R}^3$의 세 단위벡터 $\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3$에 의하여 결정되는 평행육면체의 부피가 $\frac12$이다. 두 벡터 $\mathbf{v}_i,\mathbf{v}_j$가 이루는 사잇각이 $\theta_{ij}$일 때, $ij$-성분이 $\cos\theta_{ij}$인 $3\times3$ 행렬 $A$의 행렬식의 값을 계산하여라.

### 제2차 문제

#### 2-1.

3차원 공간에서 다음 영역의 부피를 구하여라.

$$ x=(p-q)\cos t, \qquad y=(p-q)\sin t, \qquad z=p+q $$

$$ 0\le t\le2\pi, \qquad 1-\varepsilon\le p\le1+\varepsilon, \qquad -\delta\le q\le\delta, \qquad \delta<1-\varepsilon $$

#### 2-2.

다음 적분값을 가장 작게 만드는 상수 $a,b$를 구하여라.

$$ \int_0^\pi[\sin x-(ax+b)]^2\,dx $$

#### 2-3.

일차독립인 $\mathbb{R}^3$의 세 벡터 $\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3$가 다음 조건을 만족시킨다.

$$ \lVert\mathbf{v}_i\rVert=1, \qquad \mathbf{v}_i\cdot\mathbf{v}_j=-\frac13\quad(i\ne j) $$

이때 $\lVert\mathbf{x}\rVert=1$인 벡터 $\mathbf{x}$에 대하여 다음 식의 최댓값과 최솟값을 구하여라.

$$ \sum_{i=1}^3\lVert\mathbf{x}-\mathbf{v}_i\rVert^2 $$

#### 2-4.

실수 $y$에 대하여 다음 적분을 계산하여라.

$$ \int_{-\infty}^{\infty}\frac{e^{-2\pi iyx}}{\cosh\pi x}\,dx $$

#### 2-5.

양의 실수 $p$에 대하여 다음 극한값을 구하여라.

$$ \lim_{n\to\infty}\frac1{n^2} \sum_{k=1}^{n} \left(1^p+2^p+3^p+\cdots+k^p\right)^{1/(p+1)} $$

---

## 2008년 제2회 전국 대학생 공학수학 경시대회

### 제1차 문제

#### 1-1.

꼭지점의 좌표가 $(0,1,0)$, $(1,2,1)$, $(1,3,3)$, $(3,1,2)$인 사면체의 부피를 구하여라.

#### 1-2.

다음 적분을 계산하여라.

$$ \int_0^{\infty}\frac{e^{-x}-e^{-2x}}{x}\,dx $$

#### 1-3.

다음 적분을 계산하여라.

$$ \int_0^{\sqrt{\pi/2}} \int_0^{\sqrt{\pi/2}} \int_0^{\sqrt{\pi/2}} xyz\cos(x^2+y^2+z^2)\,dx\,dy\,dz $$

#### 1-4.

$\alpha>1$일 때, 다음 적분값이 존재함을 보여라.

$$ \int_1^{\infty}\cos(x^\alpha)\,dx $$

#### 1-5.

다음을 만족시키는 행렬 $A$를 하나만 구하여라.

$$ \begin{bmatrix} 2&-1\cr -1&2 \end{bmatrix} =A^2 $$

#### 1-6.

다음을 만족시키는 함수 $f(x,y,z)$를 구하여라.

$$ \nabla f(x,y,z) = \left( ye^{xy+yz}, (x+z)e^{xy+yz}, ye^{xy+yz} \right) $$

#### 1-7.

집합 $\lbrace(x,y)\mid x^2+y^2=1\rbrace$에서 함수

$$ f(x,y)=3x^2+4xy+y^2 $$

의 최댓값을 구하여라.

#### 1-8.

다음을 만족시키는 $3\times1$ 행렬 $\mathbf{u}$와 $\mathbf{v}$를 모두 구하여라.

$$ \begin{bmatrix} 5&2&3\cr 15&6&9\cr 10&4&6 \end{bmatrix} =\mathbf{u}\mathbf{v}^{T} $$

### 제2차 문제

#### 2-1.

$n\times n$ 행렬 $A=[a_{ij}]$의 원소가 임의의 $j=1,2,\ldots,n$에 대하여

$$ \sum_{i=1}^{n}a_{ij}=0 $$

을 만족할 때, $A$의 역행렬은 존재하지 않음을 보여라.

#### 2-2.

폐구간 $[0,1]$에서 정의된 함수열 $\phi_n$이 다음을 만족시킨다.

1. $\phi_0(t)=0$.
2. 다음 관계가 $n=1,2,\ldots$에 대하여 성립한다.

$$ \phi_n(t)=1+2\sin\frac{t}{2}-\frac12\int_0^t\phi_{n-1}(s)\,ds $$

폐구간 $[0,1]$에서 $\lim_{n\to\infty}\phi_n=\phi$이고 $\phi$는 미분가능한 함수일 때, 함수 $\phi(t)$를 구하여라.

#### 2-3.

다음 조건을 모두 만족시키는 다항함수 $u(x,y)$를 모두 구하여라.

1. $u_{xx}+u_{yy}=0$.
2. 모든 실수 $x$에 대하여 $u(x,0)=u(x,\sqrt{3}\,x)=0$.
3. 모든 $x>0$, $0<y<\sqrt{3}\,x$에 대하여 $u(x,y)>0$.
4. 모든 실수 $t$에 대하여 $u(tx,ty)=t^nu(x,y)$를 만족시키는 양의 정수 $n$이 존재한다.

#### 2-4.

모든 $n=0,1,2,3,\ldots$에 대해서 $a_n$은 0 또는 1이고, $P$와 $Q$는 다항식으로서 $P$의 차수가 $Q$의 차수보다 낮으며 $|x|<1$인 영역에서 다음이 성립한다.

$$ \sum_{n=0}^{\infty}a_nx^n=\frac{P(x)}{Q(x)} $$

이때 적당한 자연수 $p$가 존재하여 모든 $n\ge0$에 대하여 $a_{n+p}=a_n$이 성립함을 보여라.

#### 2-5.

다음에 주어진 네 개의 자료 $(x_i,y_i)$, $i=1,2,3,4$에 대하여 오차의 제곱의 합을 가장 작게 만드는 직선 $y=ax+b$를 구하여라.

$$ (0,0),\ (1,1),\ (3,2),\ (4,5) $$

즉, 다음 값을 가장 작게 만드는 직선 $y=ax+b$를 구하여라.

$$ E^2=\sum_{i=1}^{4}(y_i-ax_i-b)^2 $$

---

## 2009년 제28회

#### 1.

$-1\le x\le1$일 때, 곡선

$$ y=\frac12x^2 $$

의 길이를 구하여라.

#### 2.

평면에서

$$ x^{2/3}+y^{2/3}=1 $$

로 둘러싸인 영역의 넓이를 구하여라.

#### 3.

행렬

$$ A= \begin{pmatrix} -3&1&4\cr -4&2&4\cr -1&1&2 \end{pmatrix} $$

에 대하여 $A^{2009}$의 모든 성분의 합을 구하여라.

#### 4.

모든 성분이 실수인 $n\times1$ 열벡터 $\mathbf{x},\mathbf{y}$는 다음 두 조건을 모두 만족시킨다.

1. $\mathbf{x}\ne\mathbf{0}$이고 $\mathbf{y}\ne\mathbf{0}$이다.
2. $A=\mathbf{x}\mathbf{y}^{T}$는 대칭행렬이다.

이때 $A=\mathbf{u}\mathbf{u}^{T}$ 또는 $A=-\mathbf{u}\mathbf{u}^{T}$를 만족시키는, 모든 성분이 실수인 $n\times1$ 열벡터 $\mathbf{u}$가 존재함을 보여라. 단, $\mathbf{y}^{T}$는 $\mathbf{y}$의 전치(transpose)이다.

#### 5.

다음 세 조건을 모두 만족시키는 함수 $f:\mathbb{R}\to\mathbb{R}$를 모두 구하여라.

1. 모든 실수 $x$에 대하여 $f(x)>0$.
2. $f'(x)-6f(x)f'(x)-f'''(x)=0$.
3. 다음 극한들이 성립한다.

$$ \lim_{x\to\infty}f(x) = \lim_{x\to\infty}f'(x) = \lim_{x\to\infty}f''(x) =0 $$

#### 6.

다음 적분값을 구하여라.

$$ \int_{-\pi}^{\pi} \frac{x^2}{1+\sin x+\sqrt{1+\sin^2x}}\,dx $$

#### 7.

모든 양의 정수 $n$에 대하여 다음 부등식이 성립함을 보여라.

$$ \left(\frac23\right)^{2n} +\left(\frac25\right)^{2n} +\left(\frac27\right)^{2n} +\cdots <\frac1n $$

#### 8.

적분 가능한 함수 $f:[-\pi,\pi]\to\mathbb{R}$가

$$ \int_{-\pi}^{\pi}(f(x))^2\,dx<\infty $$

를 만족시킨다. 음이 아닌 정수 $k$에 대하여

$$ a_k=\int_{-\pi}^{\pi}f(x)\cos kx\,dx, \qquad b_k=\int_{-\pi}^{\pi}f(x)\sin kx\,dx $$

라 할 때,

$$ \lim_{k\to\infty}a_k = \lim_{k\to\infty}b_k =0 $$

임을 보여라.

---

## 2010년 제29회

#### 1.

다음 적분을 구하여라.

$$ \int_0^{\infty}\frac{dx}{x^3+1} $$

#### 2.

곡선

$$ y=\frac{x^2}{2}, \qquad 0\le x\le1 $$

을 $x$축을 중심으로 회전시켜 생기는 곡면의 넓이를 구하여라.

#### 3.

$x>1$에서 미분방정식

$$ (x-1)y''-xy'+y=0 $$

의 일반해를 구하여라.

#### 4.

$n\ge3$일 때, 다음 연립방정식을 풀어라.

$$ \begin{pmatrix} 1&2&3&\cdots&n\cr 2&3&4&\cdots&1\cr 3&4&5&\cdots&2\cr \vdots&\vdots&\vdots&\ddots&\vdots\cr n&1&2&\cdots&n-1 \end{pmatrix} \begin{pmatrix} x_1\cr x_2\cr x_3\cr \vdots\cr x_n \end{pmatrix} = \begin{pmatrix} n\cr n-1\cr n-2\cr \vdots\cr 1 \end{pmatrix} $$

#### 5.

두 정사각행렬 $A$와 $B$가

$$ A+B=AB $$

를 만족하면 $AB=BA$임을 보여라.

#### 6.

$x=1$에서 극값을 갖는 연속함수 $y=f(x)$가 $x=1$ 근방에서

$$ y^3+3xy^2+x^3y=1 $$

을 만족한다. $x=1$에서 $f(x)$의 2차 테일러다항식을 구하고, $f(1)$이 극댓값인지 극솟값인지 판정하여라.

#### 7.

1. $f\left(\cos\frac{\pi}{7}\right)=0$인 정수 계수 3차 다항식 $f(x)$를 구하여라.
2. 다음을 보여라.

$$ 4\sin\frac{2\pi}{7}-\tan\frac{\pi}{7}=\sqrt{7} $$

#### 8.

실수 $a$에 대하여 다음 적분을 구하여라.

$$ \int_0^{\infty}e^{-x^2}\cos(ax)\,dx $$

---

## 2011년 제30회

#### 1.

함수

$$ f(x)=\frac1{x-x^{3/5}} $$

에 대하여 다음 정적분의 값을 구하여라.

$$ \int_{2^5}^{3^5}f(x)\,dx $$

#### 2.

임의의 $n\times n$ 행렬 $A$에 대하여

$$ \det \begin{pmatrix} A&A^2\cr A^3&A^4 \end{pmatrix} =0 $$

임을 보여라. 단, $A$의 모든 성분은 실수이다.

#### 3.

함수

$$ f(x)=e^{\cos(x^2)} $$

에 대하여 다음 값을 구하여라.

$$ \frac{d^8f}{dx^8}(0) $$

#### 4.

수열 $\lbrace y_n\rbrace$은 $y_1=1$, $y_2=3$이고, 임의의 양의 정수 $n$에 대하여

$$ y_{n+2} = \frac2n\left((n^2+3n+1)y_{n+1}-2(n+1)^2y_n\right) $$

을 만족한다. 이 수열의 모든 항은 양의 정수임을 보여라.

#### 5.

함수 $f(x)$가 3차 다항식이라고 할 때, 다음을 증명하여라.

$$ \int_0^{2n}f(x)\,dx = \frac13\bigl( f(0)+4f(1)+2f(2)+4f(3)+2f(4)+\cdots +2f(2n-2)+4f(2n-1)+f(2n) \bigr) $$

#### 6.

주어진 $n\times n$ 행렬 $A=(a_{ij})$가 $1\le i,j\le n$에 대하여 다음을 만족한다.

대각성분은 다음과 같다.

$$ a_{ii}=i^2-1 $$

대각성분이 아닌 원소는 다음과 같다.

$$ a_{ij}=ij \qquad (i\ne j) $$

이 행렬 $A$의 모든 고유치(eigenvalue)들과 행렬식을 구하여라.

#### 7.

임의의 연속함수 $f:[0,1]\to[0,\infty)$에 대하여 $I(f)$를 다음과 같이 정의한다.

$$ I(f):=\int_0^1\left(x^2f(x)-(f(x))^3\right)\,dx $$

이때 $I(f)$가 가질 수 있는 가장 큰 값을 구하여라.

#### 8.

좌표평면 위에 중심이 원점이고 반지름의 길이가 1인 원판 $D$가 있다.

1. 다음 이중적분의 값을 구하여라.

$$ \iint_D\frac1{\sqrt{1-x^2-y^2}}\,dx\,dy $$

2. 가로의 길이가 2011이고 세로의 길이가 각각 $a_1,\ldots,a_n$인 $n$개의 직사각형 종이조각들이 주어져 있다. 만약

$$ \sum_{i=1}^{n}a_i<2 $$

이면, 이 종이들로 원판 $D$를 덮을 수 없음을 보여라. 단, 종이들을 겹치는 것은 허용하되 접거나 찢는 것은 허용하지 않는다.

---

## 2012년 제31회

#### 1.

다음과 같이 주어진 행렬 $A$에 대하여 $A^{2012}$의 모든 성분의 합을 구하여라.

$$ A= \begin{pmatrix} -2&1&2\cr -4&3&2\cr -4&1&4 \end{pmatrix} $$

#### 2.

다음 조건을 만족하는 연속함수 $f:[0,1]\to[0,\infty)$를 모두 구하여라.

$$ \int_0^1xf(x)\,dx = 3\int_0^1x^2f(x)\,dx = 9\int_0^1x^3f(x)\,dx $$

#### 3.

좌표평면상에 주어진 직선 $y=x$와 곡선 $y=x^2$으로 둘러싸인 부분을 $y=x$를 축으로 하여 회전하였을 때 생기는 회전체의 부피를 구하여라.

#### 4.

두 점 $P,Q$를 다음과 같이 정의한다.

$$ P=(0,1), \qquad Q=(2\sqrt{3},3) $$

$X$가 $x$축 위의 임의의 점일 때, 다음 식의 최솟값을 구하여라.

$$ |X-P|+\sqrt{3}|X-Q| $$

#### 5.

다음 조건을 만족하는 모든 $n\times n$ 행렬 $A$에 대하여 $A$의 대각합이 될 수 있는 값을 모두 구하여라. 단, $I$는 단위행렬이다.

$$ A^2=I, \qquad \mathrm{rank}(A+I)=1 $$

#### 6.

실수 $x,y,z$에 대하여

$$ m_k=\frac13\left(x^k+y^k+z^k\right) $$

으로 정의한다. 만약 $m_1=0$이면 다음 부등식이 성립함을 보여라.

$$ m_3^2\le m_2\left(m_4-m_2^2\right) $$

#### 7.

수열 $\lbrace e_n\rbrace$을

$$ e_n=\left(1+\frac1n\right)^n $$

으로 정의할 때, 다음 극한값을 계산하여라. 단, $e$는 자연로그의 밑이다.

$$ \lim_{n\to\infty} \left(\frac{2n(e-e_n)}{e}\right)^n $$

#### 8.

모든 성분이 실수인 대칭행렬 $A$가 다음 조건을 만족시킬 때, $A$의 어떤 고유치는 1보다 크다는 것을 보여라.

1. $A$의 모든 대각성분은 0이다.
2. $A$의 어떤 성분은 1보다 크다.

---

## 2013년 제32회

#### 1.

다음 우극한값을 구하여라.

$$ \lim_{a\to0^+}\sqrt{a} \iiint_{\mathbb{R}^3} \frac1{\left(\sqrt{x^2+y^2+z^2}+a\right)^{7/2}} \,dx\,dy\,dz $$

#### 2.

양의 정수 $n\ge2$와 실수 $a,b$에 대하여, $n\times n$ 행렬 $M_n(a,b)$의 대각성분은 모두 $a$이고 나머지 성분은 모두 $b$이다. 행렬 $M_n(a,b)$의 행렬식을 구하여라.

#### 3.

삼차원 공간 $\mathbb{R}^3$에 다섯 개의 점

$$ A=(2,0,-1), \quad B=(-1,3,-1), \quad C=(-3,1,1), \quad P=(-2,1,1), \quad Q=(1,1,-2) $$

가 주어져 있다.

1. 선분 $PQ$가 삼각형 $ABC$의 내부와 만남을 보여라.
2. 다섯 개의 점 $A,B,C,P,Q$를 꼭지점으로 갖고, 모든 면이 삼각형인 볼록 육면체의 부피를 구하여라.

#### 4.

다음 두 적분에 대한 등식

$$ \int_{\mathbb{R}}\frac{x^2}{x^4+1}\,dx = \int_{\mathbb{R}}\frac1{x^4+1}\,dx $$

이 성립함을 보이고, 그 공통된 적분값을 구하여라.

#### 5.

임의의 $n\times n$ 정사각행렬 $A,B$와 $n\times n$ 단위행렬 $I$에 대하여 다음을 증명하여라.

$$ \det(I+AB)=\det(I+BA) $$

#### 6.

단위원 위에 서로 다른 네 점을 무작위로 잡아 그 네 점을 꼭지점으로 하는 사각형들을 생각하자. 이때 이러한 사각형들의 넓이의 평균값을 구하여라.

#### 7.

크기가 $n\times n$인 실행렬 $A$의 모든 행벡터의 크기는 1이고

$$ \mathrm{tr}(A)>\sqrt{n(n-1)} $$

일 때, 다음을 보여라.

$$ \det(A)\ne0 $$

#### 8.

모든 실수 $x$에 대하여 다음 부등식을 만족시키는, $x$와 무관한 양의 상수 $C$가 존재함을 보여라.

$$ \sum_{n=1}^{\infty} \frac{|x|}{n^2\left(|n-x|+1\right)^2} <C $$

---

## 2014년 제33회

#### 1.

양의 정수 $n$과 함수

$$ f(x)=\frac{e^x}{x-1} $$

에 대하여

$$ f^{(n)}(x)=\frac{P_n(x)e^x}{(x-1)^{n+1}} $$

를 만족하도록 다항식 $P_n(x)$를 정의하자. 이때 $P_n(1)$과 $P_n'(1)$을 구하여라.

#### 2.

모든 항이 실수인 수열 $\lbrace a_n\rbrace$과 $\lbrace b_n\rbrace$은

$$ \lim_{n\to\infty}(a_n+b_n)=0, \qquad \lim_{n\to\infty}(e^{a_n}+e^{b_n})=2 $$

를 만족한다. 수열 $\lbrace a_n\rbrace$이 수렴함을 보여라.

#### 3.

양의 정수 $m$과 임의의 $n\times n$ 실행렬 $A$에 대하여

$$ A^m=O $$

이면 다음 부등식이 성립함을 보여라.

$$ \mathrm{rank}(A)\le\frac{m-1}{m}n $$

단, $O$는 영행렬이다.

#### 4.

다음 적분값을 구하여라.

$$ \iiint_{\mathbb{R}^3} e^{-3x^2-3y^2-2z^2-2xz+2yz} \,dx\,dy\,dz $$

#### 5.

양의 정수 $n$ $(n\ge2)$에 대하여 $n\times n$ 실행렬로 이루어진 실벡터공간을 $M_n(\mathbb{R})$이라 하자. 다음 조건을 모두 만족시키는 부분집합 $V$는 $M_n(\mathbb{R})$과 같음을 보여라. 단, $E_{ij}$는 $i$행과 $j$열이 만나는 성분만 1이고 나머지는 모두 0인 행렬이다.

1. $V$는 $M_n(\mathbb{R})$의 부분공간이다.
2. $A,B\in V$이면 $AB\in V$이다.
3. 다음 두 행렬이 $V$에 속한다.

$$ E_{12}+E_{23}+\cdots+E_{n-1,n} $$

$$ E_{21}+E_{32}+\cdots+E_{n,n-1} $$

#### 6.

함수 $f:\mathbb{R}\to\mathbb{R}$는 두 번 미분가능하고 $f(0)=0$을 만족한다. 다음 부등식을 증명하여라.

$$ \int_0^1|f(x)f'(x)|\,dx \le \frac12\int_0^1|f'(x)|^2\,dx $$

#### 7.

실행렬 $A,B$는 고유치가 모두 양수인 $n\times n$ 대칭행렬이다. 임의의 $n\times n$ 실행렬 $C$에 대하여 다음 $2n\times2n$ 행렬은 역행렬을 가짐을 보여라. 단, $C^T$는 $C$의 전치행렬이다.

$$ \begin{pmatrix} A&C\cr C^T&-B \end{pmatrix} $$

#### 8.

연속함수 $f:[a,b]\to\mathbb{R}$는 임의의 $t\in[0,1]$와 $x,y\in[a,b]$에 대하여

$$ f(tx+(1-t)y)\le tf(x)+(1-t)f(y) $$

를 만족한다. 단, $a<b$이다. 다음 부등식을 증명하여라.

$$ f\left(\frac{a+b}{2}\right) \le \frac1{b-a}\int_a^bf(x)\,dx \le \frac{f(a)+f(b)}{2} $$

---

## 2015년 제34회

#### 1.

다음 적분값을 계산하여라.

$$ \int_0^{\infty}\left(x^2+1-x\sqrt{x^2+2}\right)\,dx $$

#### 2.

양의 정수 $n$에 대하여 크기가 $n\times n$인 행렬 $A=(a_{ij})$가

$$ a_{ij}=\max\lbrace i,j\rbrace $$

로 주어질 때, $\det(A)$를 구하여라.

#### 3.

다음 급수의 수렴 여부를 판정하여라.

$$ \sum_{n=1}^{\infty}\left((2n+1)e^{-1/n}-(2n-1)\right) $$

#### 4.

모든 성분이 유리수이고 크기가 $5\times5$인 행렬 $A$의 한 고유벡터가

$$ (1,\sqrt2,\sqrt3,\sqrt4,\sqrt5)^T $$

이다. 이때 $A^T$는 모든 성분이 유리수인 고유벡터를 가짐을 보여라. 단, $A^T$는 $A$의 전치행렬이다.

#### 5.

무한 번 미분가능한 함수 $f:(-1,\infty)\to\mathbb{R}$가 모든 양의 정수 $n$에 대하여

$$ f\left(\frac1n\right)=\frac{n}{n+1} $$

을 만족할 때, $f'''(0)$을 구하여라.

#### 6.

함수 $f:[a,b]\to\mathbb{R}$가 연속일 때, 다음을 만족하는 상수 $c\in[a,b]$가 존재함을 보여라.

$$ \int_a^b f(x)e^x\,dx=e^a\int_a^c f(x)\,dx+e^b\int_c^b f(x)\,dx $$

#### 7.

양의 정수 $n$에 대하여 크기가 $n\times n$인 복소행렬 $A=(a_{ij})$가 다음 조건을 만족한다.

1. $a_{ii}=1$ $(1\le i\le n)$
2. $a_{ij}=0$ $(1\le j<i\le n)$
3. $\overline{A}A=I$

이때 다음 조건을 만족하는 크기가 $n\times n$인 복소행렬 $B=(b_{ij})$가 존재함을 보여라.

1. $b_{ii}=1$ $(1\le i\le n)$
2. $b_{ij}=0$ $(1\le j<i\le n)$
3. $\overline{B}A=B$

단, $\overline{A}=(\overline{a_{ij}})$이고 $I$는 단위행렬이다.

#### 8.

양의 정수 $n$에 대하여 크기가 $n\times n$인 실행렬 $X=(x_{ij})$의 성분 $x_{ij}$들은 $X$가 가역인 범위에 있다고 가정하자. 역행렬 $Y=X^{-1}=(y_{pq})$의 성분을

$$ y_{pq}=y_{pq}(x_{11},x_{12},\ldots,x_{nn}) $$

과 같이 변수 $x_{ij}$에 대한 함수로 생각할 때, 다음을 보여라.

$$ \frac{\partial y_{pq}}{\partial x_{ij}}=-y_{pi}y_{jq} $$

---

## 2016년 제35회

#### 1.

다음 극한값을 구하여라. 단, $\log$는 자연로그이다.

$$ \lim_{n\to\infty}\frac1n\log\left(\sum_{k=2}^{2^n}k^{1/n^2}\right) $$

#### 2.

실수 $a,b$가 $a^2+b^2\le\frac{\pi^2}{4}$를 만족할 때, 다음 부등식을 증명하여라.

$$ \cos\sqrt{a^2+b^2}\le\cos a\cos b $$

#### 3.

양의 정수 $n$에 대하여 크기가 $n\times n$인 실행렬 $A$가 서로 다른 $n$개의 양의 고유치를 가질 때,

$$ B^{2016}=A $$

를 만족하는 실행렬 $B$의 개수를 구하여라.

#### 4.

다음 미분방정식을 만족하는 함수 $y(t)$에 대하여 $y\left(\frac\pi4\right)$를 구하여라.

$$ y'(t)=y(t)-\int_0^t y'(s)^2\cos s\,ds, \qquad y(0)=2 $$

#### 5.

연속함수 $f:[-\frac\pi4,\frac\pi4]\to[-1,1]$가 구간 $(-\frac\pi4,\frac\pi4)$에서 미분가능할 때, 다음 부등식을 만족하는 점 $x_0$가 이 구간에 존재함을 보여라.

$$ |f'(x_0)|\le1+f(x_0)^2 $$

#### 6.

양의 정수 $m,n$에 대하여 크기가 $m\times n$인 실행렬 $A$가 주어져 있다.

1. 행렬 $X=I_m+AA^T$와 $Y=I_n+A^TA$는 가역임을 보여라.
2. 다음 값을 구하여라.

$$ \mathrm{tr}(X^{-1})-\mathrm{tr}(Y^{-1}) $$

단, $A^T$는 $A$의 전치행렬이고, 양의 정수 $\ell$에 대하여 $I_\ell$은 크기가 $\ell\times\ell$인 단위행렬이다.

#### 7.

양의 실수 $x_1,x_2,x_3$와 실수 $a\in[-\frac12,1]$에 대하여

$$ m=\frac{x_1+x_2+x_3}{3}, \qquad y_i=(1-a)m+ax_i \quad (i=1,2,3) $$

라고 할 때, 다음 부등식을 증명하여라.

$$ \frac{27y_1y_2y_3}{(y_1+y_2+y_3)^3}\ge\left(\frac{27x_1x_2x_3}{(x_1+x_2+x_3)^3}\right)^{\max\lbrace a,-2a\rbrace} $$

#### 8.

양의 정수 $n$에 대하여 크기가 $n\times n$인 실행렬 $A,B$가

$$ A\ne O, \qquad AB-BA=A $$

를 만족할 때, $A^n=O$이고 $B^n\ne O$임을 보여라. 단, $O$는 영행렬이다.

---

## 2017년 제36회

#### 1.

차수가 3 이하인 모든 실계수 다항식으로 이루어진 실벡터공간을 $V$라 하고, 선형사상 $T:V\to V$를 다음과 같이 정의한다.

$$ T(P(x))=x^4P(x)\text{를 }(x-1)^2(x+1)^2\text{으로 나눈 나머지} $$

이때 $T$의 특성다항식을 구하여라.

#### 2.

다음 미분방정식의 해 $u(t)$를 구하여라.

$$ u'(t)=-u(t)+u(t)^2e^t \quad (t>0), \qquad u(0)=-1 $$

#### 3.

양의 정수 $n$에 대하여 $n\times n$ 실행렬 $A$가

$$ A^{n+1}=O $$

를 만족하면 $A^n=O$임을 보여라. 단, $O$는 영행렬이다.

#### 4.

계수가 유리수인 다항식 $f(x,y)$는 임의의 정수 $m,n$에 대하여 $f(m,n)$이 정수이다. 이때 다항식 $f(x,y)$는 적당한 정수 $c_{ij}$에 대하여 다음과 같이 표현됨을 보여라.

$$ f(x,y)=\sum_{i,j\ge0}c_{ij}\binom{x}{i}\binom{y}{j} $$

단,

$$ \binom{x}{i}=\frac{x(x-1)\cdots(x-i+1)}{i!}, \qquad \binom{x}{0}=1 $$

이다.

#### 5.

행렬

$$ A=\begin{pmatrix}2&2&3\cr0&3&3\cr4&4&4\end{pmatrix} $$

에 대하여 실행렬 $B$가 $AB=BA$를 만족하면, 적당한 실수 $a,b,c$에 대하여

$$ B=aA^2+bA+cI $$

가 성립함을 보여라.

#### 6.

양의 정수 $n$ $(n\ge3)$에 대하여 $n\times n$ 실행렬 $A=(a_{ij})$가 다음 조건을 만족하면 $A$는 가역행렬임을 보여라.

1. 모든 $i,j$에 대하여 $a_{ij}>0$이다.
2. 모든 $i$에 대하여 다음이 성립한다.

$$ 2a_{ii}=\sum_{j=1}^n a_{ij} $$

#### 7.

수열 $\lbrace a_n\rbrace$은 모든 항이 양수인 감소수열이고 $0<\theta<\frac\pi2$이다. 다음 부등식을 증명하여라.

$$ \left|\sum_{n=1}^{2017}a_n\cos(n\theta)\right|\le\frac{\pi a_1}{\theta} $$

#### 8.

다음 극한값을 구하여라.

$$ \lim_{n\to\infty}n\int_0^{\pi/2}(1-\sin x)^n\,dx $$

---

## 2018년 제37회

#### 1.

좌표평면 위의 두 직선 $y=(1-h)x$와 $y=(1+h)x$ 사이의 각을 $\theta(h)$라 하자. 단, $\theta(h)\in[0,\frac\pi2]$이다. 다음 극한값을 구하여라.

$$ \lim_{h\to0^+}\frac{\theta(h)}{h} $$

#### 2.

다음 미분방정식의 해를 구하여라.

$$ \frac{dy}{dx}=x^3(x+y)^2-\frac{3(x+y)}{x}-1, \qquad y(2)=-3 $$

#### 3.

크기가 $2\times2$인 실행렬들로 이루어진 실벡터공간을 $V$라 하자. 행렬 $A\in V$가

$$ \mathrm{tr}(A)=3, \qquad \mathrm{tr}(A^2)=5 $$

를 만족할 때, $V$에서 정의되는 선형사상 $T(B)=AB+BA$에 대하여 $\mathrm{tr}(T^3)$을 구하여라.

#### 4.

지구 북반구 위의 두 지점 $A$와 $B$의 위치는 각각 $(\text{위도 }\alpha,\text{ 경도 }0)$과 $(\text{위도 }\beta,\text{ 경도 }\frac\pi2)$이다. 단, 지구는 구라고 가정하고 각도의 단위는 라디안이며 $0<\alpha,\beta<\frac\pi2$이다. 지점 $A$에서 지점 $B$까지 두 점을 지나는 지표면 위의 대원을 따라 북반구 위를 이동할 때, 가장 높은 위도를 지나는 순간의 경도를 $\gamma$라 하자. 이때 $\tan\gamma$를 $\alpha$와 $\beta$에 관한 식으로 표현하여라.

#### 5.

정수 $1,2,\ldots,n$을 재배열하여 얻어지는 임의의 두 순열 $(\alpha_1,\ldots,\alpha_n)$과 $(\beta_1,\ldots,\beta_n)$에 대하여 $n\times n$ 행렬 $A=(a_{ij})$는

$$ a_{ij}=(1+\alpha_i\beta_j)^{n-1} $$

로 정의된다. 행렬식 $\det(A)$가 취할 수 있는 값을 모두 구하여라. 단, $n\ge2$이다.

#### 6.

다음 급수의 값을 구하여라.

$$ \sum_{n=1}^{\infty}\sum_{m=1}^{\infty}\sum_{k=0}^{\infty}\frac{(-1)^{n+k-1}}{n^{k+2}2^{m(k+1)}} $$

#### 7.

수열 $\lbrace a_n\rbrace$은 임의의 양의 정수 $n$에 대하여 다음과 같이 정의된다.

$$ a_n=\frac{1}{(1+\sqrt[3]{2})^n}\sum_{k=0}^{\infty}\binom{n}{3k+2}2^k $$

다음 극한값을 구하여라. 단, $\binom{n}{3k+2}={}_nC_{3k+2}$이고 $3k+2>n$이면 $\binom{n}{3k+2}=0$이다.

$$ \lim_{n\to\infty}a_n $$

#### 8.

두 번 미분가능한 함수 $f:\mathbb{R}\to(0,\infty)$가 다음 조건을 만족한다.

$$ \int_{-\infty}^{\infty}f(x)\,dx=1, \qquad \int_{-\infty}^{\infty}xf(x)\,dx=0, \qquad \int_{-\infty}^{\infty}x^2f(x)\,dx=1 $$

다음 부등식이 성립함을 보이고, 등호가 성립하도록 하는 함수 $f$를 모두 구하여라.

$$ \int_{-\infty}^{\infty}\frac{f'(x)^2}{f(x)}\,dx\ge1 $$

---

## 2019년 제38회

#### 1.

다음 극한값을 구하여라.

$$ \lim_{n\to\infty}n\sin\left(\sin\left(\sin\left(\frac{2019}{n}\right)\right)\right) $$

#### 2.

다음 미분방정식의 해를 구하여라.

$$ x'(t)=x(t)(x(t)y(t)-1), \qquad y'(t)=y(t)(x(t)y(t)-1), \qquad x(0)=\frac12, \quad y(0)=1 $$

#### 3.

영역

$$ W=\lbrace(x,y,z)\in\mathbb{R}^3:x^2+y^2\le1,\ |z|\le1\rbrace $$

에서 정의된 벡터장

$$ \mathbf{F}(x,y,z)=(\sin(xy),\sin(yz),0), \qquad \mathbf{G}(x,y,z)=(e^{x^2+y^2+z^2},\cos(xz),0) $$

에 대하여 다음 적분값을 구하여라. 단, $\mathrm{curl}(\mathbf{F})=\nabla\times\mathbf{F}$이다.

$$ \iiint_W\left(\mathbf{G}\cdot\mathrm{curl}(\mathbf{F})-\mathbf{F}\cdot\mathrm{curl}(\mathbf{G})\right)\,dV $$

#### 4.

크기가 $n\times n$인 행렬 $A=(a_{ij})$는

$$ a_{ij}=x^{|i-j|} $$

로 정의된다. 단, $x$는 양의 실수이다.

1. 행렬식 $\det(A)$의 값을 구하여라.
2. 행렬 $A$가 가역이 되는 $x$를 모두 구하고, 그때 $A$의 역행렬을 구하여라.

#### 5.

크기가 $n\times n$인 실행렬들로 이루어진 실벡터공간을 $M_n(\mathbb{R})$이라 하자. 행렬 $A,B\in M_n(\mathbb{R})$에 대하여 선형사상 $L_{A,B}:M_n(\mathbb{R})\to M_n(\mathbb{R})$을

$$ L_{A,B}(X)=AXB $$

로 정의하면 다음이 성립함을 보여라.

$$ \det(L_{A,B})=\det(A^nB^n) $$

#### 6.

실수 위에서 정의된 함수

$$ f(x)=\frac{\sqrt3(2x+1)}{(x^2+x+3)^2+3} $$

에 대하여 다음 급수의 값을 구하여라.

$$ \sum_{n=1}^{\infty}\left(\frac\pi6-\int_0^n f(x)\,dx\right) $$

#### 7.

벡터 $\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3,\mathbf{v}_4\in\mathbb{R}^4$는 길이가 각각 $2,3,4,5$이며 서로 수직이다. 임의의 2차원 부분공간 $W\subset\mathbb{R}^4$에 대하여 $\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3,\mathbf{v}_4$를 $W$에 정사영하여 얻은 벡터들 가운데 적어도 하나는 길이가 1이 아님을 보여라.

#### 8.

실수 $a$와 정수 $n$ $(n\ge2)$에 대하여 $S_n(a)$를 다음과 같이 정의하자.

$$ S_n(a)=n^a\sum_{k=1}^{n-1}\frac{1}{k^{2019}(n-k)^{2019}} $$

수열 $\lbrace S_n(a)\rbrace_{n\ge2}$가 양의 실수로 수렴하는 $a$의 값을 모두 구하여라.

---

## 2020년 제39회

#### 1.

다음 극한값을 구하여라.

$$ \lim_{x\to0}\frac{e^x\sin(x^2)-x^2}{x^3} $$

#### 2.

양의 정수 $n\ge2$에 대하여 $n\times n$ 대칭행렬 $M=(m_{ij})$을 다음과 같이 정의하자.

- $i=1$ 또는 $j=1$이면 $m_{ij}=1$이다.
- $i=j\ge2$이면 $m_{ij}=\frac{j-1}{j}$이다.
- 그 밖의 경우에는 $m_{ij}=0$이다.

이때 다음 식의 값을 $n$에 대한 다항식으로 표현하여라.

$$ n\det(M)+1+\frac12+\cdots+\frac1{n-1} $$

#### 3.

이변수함수 $f(x,y)$를 다음과 같이 정의하자.

$$ f(x,y)=3+2\cos x+2\cos y+2\cos(x-y) $$

1. 모든 실수 $x,y$에 대하여 $f(x,y)\ge0$임을 보여라.
2. 영역 $\lbrace(x,y)\in\mathbb{R}^2:-\pi\le x,y\le\pi\rbrace$에서 $f(x,y)=0$이 되는 $(x,y)$를 모두 구하여라.

#### 4.

다음 극한값을 구하여라.

$$ \lim_{n\to\infty}\int_0^{\pi/2}\frac{\cos^2x}{1+\cos^2(nx)}\,dx $$

#### 5.

다음 세 행렬이 공통의 고유벡터를 갖기 위한 $a,b$의 필요충분조건을 구하여라.

$$ X=\begin{pmatrix}a&1\cr0&b\end{pmatrix}, \qquad Y=\begin{pmatrix}b&-1\cr0&a\end{pmatrix}, \qquad Z=\begin{pmatrix}0&1\cr1&0\end{pmatrix} $$

#### 6.

세 벡터 $\mathbf{w}_i=(a_{i1},a_{i2},a_{i3})\in\mathbb{R}^3$ $(i=1,2,3)$에 대하여 행렬 $(a_{ij})_{1\le i,j\le3}$의 행렬식을 $|\mathbf{w}_1,\mathbf{w}_2,\mathbf{w}_3|$이라고 하자. 임의의 여섯 벡터 $\mathbf{u}_1,\mathbf{u}_2,\mathbf{u}_3,\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3\in\mathbb{R}^3$에 대하여 다음 등식이 성립함을 보여라.

$$ |\mathbf{u}_1,\mathbf{u}_2,\mathbf{u}_3||\mathbf{v}_1,\mathbf{v}_2,\mathbf{v}_3|=|\mathbf{v}_1,\mathbf{u}_2,\mathbf{u}_3||\mathbf{u}_1,\mathbf{v}_2,\mathbf{v}_3|+|\mathbf{u}_1,\mathbf{v}_1,\mathbf{u}_3||\mathbf{u}_2,\mathbf{v}_2,\mathbf{v}_3|+|\mathbf{u}_1,\mathbf{u}_2,\mathbf{v}_1||\mathbf{u}_3,\mathbf{v}_2,\mathbf{v}_3| $$

#### 7.

다음 극한값을 구하여라.

$$ \lim_{n\to\infty}\sum_{j=1}^n\sum_{k=j^2}^{n^2}\frac{j^3\sqrt{k^3+n^6}}{n^9} $$

#### 8.

구간 $[0,\infty)$에서 정의된 미분가능한 실함수 $x(t),y(t)$가 $(x(0),y(0))=(2020,2020)$이고 모든 양수 $t$에 대하여

$$ x'(t)=-2x(t)y(t)+3x(t)+1, \qquad y'(t)=x(t)^2-x(t)-y(t)+2 $$

를 만족한다고 하자. 다음 극한값이 존재함을 보이고 그 값을 구하여라.

$$ \lim_{t\to\infty}(x(t)+y(t)) $$

---

## 2021년 제40회

#### 1.

다음 극한값을 구하여라.

$$ \lim_{x\to0}\frac{e^{x^2}-e^{-x^2}}{1-\cos x} $$

#### 2.

다음 미분방정식의 해를 구하여라.

$$ 2yy''+2(y')^2-8yy'+3y^2=0, \qquad y(0)=2, \quad y'(0)=3 $$

#### 3.

다음 행렬이 가역행렬이 되도록 하는 2 이상의 정수 $n$과 실수 $\alpha_1,\ldots,\alpha_n,\beta_1,\ldots,\beta_n$을 모두 구하여라.

$$ \begin{pmatrix}\cos(\alpha_1-\beta_1)&\cos(\alpha_1-\beta_2)&\cdots&\cos(\alpha_1-\beta_n)\cr\cos(\alpha_2-\beta_1)&\cos(\alpha_2-\beta_2)&\cdots&\cos(\alpha_2-\beta_n)\cr\vdots&\vdots&\ddots&\vdots\cr\cos(\alpha_n-\beta_1)&\cos(\alpha_n-\beta_2)&\cdots&\cos(\alpha_n-\beta_n)\end{pmatrix} $$

#### 4.

영역

$$ D=\lbrace(x,y)\in\mathbb{R}^2:x>0,\ y>0,\ 1-x-y>0\rbrace $$

에서 함수 $f:D\to\mathbb{R}$을 다음과 같이 정의하자.

$$ f(x,y)=4\left(x^2+y^2+(1-x-y)^2\right)-x\log x-y\log y-(1-x-y)\log(1-x-y) $$

이때 $f$의 극점의 개수를 구하여라.

#### 5.

다음 적분값을 구하여라.

$$ \int_0^1\int_0^1\int_0^1\lfloor x+y+z\rfloor\,dx\,dy\,dz $$

단, $\lfloor t\rfloor$는 실수 $t$를 넘지 않는 가장 큰 정수이다.

#### 6.

크기가 $n\times n$인 가역행렬 $A=(a_{ij})$가 $1\le k\le n$인 모든 정수 $k$에 대하여 다음을 만족한다.

$$ \sum_{i=1}^n a_{ik}=\sum_{j=1}^n a_{kj}=2021 $$

행렬 $A$의 역행렬을 $B=(b_{ij})$라 할 때, $1\le k\le n$인 모든 정수 $k$에 대하여 다음이 성립함을 보여라.

$$ \sum_{i=1}^n b_{ik}=\sum_{j=1}^n b_{kj}=\frac1{2021} $$

#### 7.

실수 $a,b$에 대하여 행렬 $A$를 다음과 같이 정의하자.

$$ A=\begin{pmatrix}a&1&1&1\cr1&b&1&1\cr1&1&a&1\cr1&1&1&b\end{pmatrix} $$

이때 행렬 $A$가 양의 정부호일 필요충분조건은 $a>1$이고 $b>1$임을 보여라.

#### 8.

실수에서 정의된 두 번 미분가능한 실함수 $f$가

$$ \lim_{x\to\infty}f(x)=0 $$

을 만족한다고 하자. 다음 부등식을 증명하여라.

$$ \int_0^{\infty}x^4f(x)^4\,dx\le2\left(\int_0^{\infty}x^2f(x)^2\,dx\right)^{3/2}\left(\int_0^{\infty}x^2f'(x)^2\,dx\right)^{1/2} $$

---

## 2023년 제41회

#### 1.

좌표공간 안에 단위구 $x^2+y^2+z^2=1$의 일부인 영역 $D$가 다음과 같이 주어졌다.

$$ D=\lbrace(x,y,z):x^2+y^2+z^2=1,\ x,y\ge0,\ \tfrac12\le z\le\tfrac{\sqrt3}{2}\rbrace $$

점 $(0,0,1)$에 놓인 광원으로부터 $D$를 $xy$-평면에 사영하여 얻은 영역의 넓이는 $D$의 넓이의 몇 배인지 구하여라.

#### 2.

다음 조건을 만족하는 열린 구간 $(\frac1e,e)$에서 정의된 함수 $y=y(x)$를 구하여라.

$$ xy'=\sqrt{x^2-y^2}+y, \qquad -x<y<x, \qquad y(1)=\frac12 $$

#### 3.

함수

$$ f(x)=e^{x^8}\sec(x^4) $$

에 대하여 다음 값을 구하여라. 단, $f^{(n)}(x)$는 $f(x)$의 $n$계도함수이다.

$$ \frac{f^{(16)}(0)}{16!} $$

#### 4.

크기가 $2\times n$ $(n\ge4)$인 실행렬 $A=(a_{ij})$와 순서쌍 $(p,q)$에 대하여

$$ f(p,q)=\det\begin{pmatrix}a_{1p}&a_{1q}\cr a_{2p}&a_{2q}\end{pmatrix} \qquad (1\le p,q\le n,\ p\ne q) $$

라고 하자.

1. 서로 다른 $p,q,r,s$에 대하여 다음이 성립함을 보여라.

$$ f(p,q)f(r,s)=f(r,q)f(p,s)+f(s,q)f(r,p) $$

2. 임의의 $q$ $(2\le q\le n)$에 대하여 $f(1,q)>0$이고 $f(q-1,q)>0$이라고 하자. 이때 $1\le p<q\le n$인 모든 순서쌍 $(p,q)$에 대하여 $f(p,q)>0$임을 보여라.

#### 5.

크기가 $n\times n$인 실행렬 $A$에 대하여

$$ \det(A+D)\ne0 $$

이 성립하도록 하는 대각행렬

$$ D=\mathrm{diag}(d_1,\ldots,d_n), \qquad d_1,\ldots,d_n\in\lbrace-1,1\rbrace $$

이 존재함을 보여라.

#### 6.

$A$와 $B$가 바닥에서 출발하여 단의 개수가 12개인 계단을 먼저 올라가면 이기는 게임을 하려고 한다. 1부터 12까지의 수가 적혀 있는 정12면체 주사위를 번갈아 던져서 다음 규칙에 따라 계단을 올라간다.

- $A$가 던져서 나온 주사위의 수가 4의 배수일 때, $A$는 4계단 올라간다.
- $B$가 던져서 나온 주사위의 수가 6의 배수일 때, $B$는 6계단 올라간다.
- 그 외의 경우에는 모두 움직이지 않는다.

$A$가 먼저 주사위를 던지기 시작할 때, $A$가 이길 확률을 구하여라. 단, 어느 한 명이 이길 때까지 계속하여 번갈아 주사위를 던진다.

#### 7.

수열 $\lbrace a_n\rbrace$이 다음 식을 만족한다.

$$ \sqrt[4]{\frac{1+4x}{1-4x}}=\sum_{n=0}^{\infty}a_nx^n, \qquad |x|<\frac14 $$

이때 모든 $n\ge0$에 대하여 $a_n$은 음이 아닌 정수가 됨을 보여라.

---

## 2024년 제42회

#### 1.

다음 행렬 $A$에 대하여

$$ A^5=aA^2+bA+cI $$

가 성립하도록 하는 실수 $a,b,c$를 구하여라. 단, $I$는 $3\times3$ 단위행렬이다.

$$ A=\begin{pmatrix}0&1&1\cr-1&0&1\cr-1&-1&0\end{pmatrix} $$

#### 2.

다음 미분방정식의 해 $y(x)$를 구하여라.

$$ \frac{dy}{dx}=-\frac{x+y-2}{x-y}, \qquad y(1)=0 $$

#### 3.

양수 $a$에 대하여 3차원 공간의 입체 $V$를 다음과 같이 정의하자.

$$ V=\lbrace(x,y,z):x^2+y^2+(z-1)^2\le1,\ z\ge\sqrt{ax^2+ay^2}\rbrace $$

입체 $V$의 부피가 $\frac{7\pi}{12}$일 때 $a$의 값을 구하여라.

#### 4.

다음 식을 만족하는 실수 $N$을 구하여라.

$$ \sum_{k=1}^{2024}(2025-k)\cdot2024^k\cdot2025^{2024-k}=2024^N $$

#### 5.

다음 이상적분의 값을 구하여라.

$$ \int_0^{\infty}\frac{1}{(x^{2024}+1)(x^2+1)}\,dx $$

#### 6.

크기가 $n\times n$인 실행렬 $A,B$에 대하여 $\mathrm{rank}(B)=1$일 때 다음 부등식을 증명하여라. 단, $n\ge2$이다.

$$ \det(A+B)\det(A-B)\le(\det A)^2 $$

#### 7.

2차원 좌표평면 위의 유한한 닫힌 볼록 영역 $D$에 대하여 $D$의 경계 $\beta$가 다음을 만족한다고 하자.

1. $\beta$는 매끄러운 단순폐곡선이다.
2. $\beta$ 위의 임의의 점 $O$에 대하여 $O$를 중심으로 하는 반지름이 1인 원은 $\beta$와 두 점에서만 만난다.
3. $\beta$ 위의 임의의 점 $O$에서 그린 접선은 $O$를 제외한 다른 점에서 $\beta$와 만나지 않는다.

길이가 1인 막대 $PQ$의 중점을 $M$이라 하자. 막대의 양 끝점 $P,Q$가 항상 $\beta$ 위에 놓이도록 막대를 한 바퀴 돌리면, 즉 곡선 $\beta$를 따라 두 점 $P,Q$를 연속적으로 한 바퀴 돌리면, $M$의 자취가 단순폐곡선 $\gamma$가 된다고 하자. 이때 두 곡선 $\beta$와 $\gamma$ 사이에 있는 영역의 넓이가 $\frac\pi4$임을 보여라.

---

## 2025년 제43회

#### 1.

다음 조건을 모두 만족하는 미분가능한 이변수함수 $f:\mathbb{R}^2\to\mathbb{R}$가 주어져 있다.

$$ f(0,0)=0, \qquad \lim_{t\to0}\frac{f(t,t)}{t}=2, \qquad \lim_{t\to0}\frac{f(t,2t)}{t}=-1 $$

$a^2+b^2=1$을 만족하는 실수 $a,b$에 대하여 다음 극한이 가질 수 있는 최댓값과 최솟값을 구하여라.

$$ \lim_{t\to0}\frac{f(at,bt)}{t} $$

#### 2.

다음 미분방정식의 해 $f$를 구하여라.

$$ f''(x)+2f'(x)+f(x)=2x^{-2}e^{-x}, \qquad f(1)=f'(1)=0 $$

#### 3.

3차원 벡터공간의 한 평면 위에 놓인 다섯 개의 점 $A,B,C,D,E$가 다음 조건을 모두 만족한다.

$$ |\overrightarrow{AB}|=|\overrightarrow{BC}|=|\overrightarrow{CD}|=|\overrightarrow{DE}|=|\overrightarrow{EA}|=1 $$

$$ \overrightarrow{AB}\times\overrightarrow{BC}=\overrightarrow{BC}\times\overrightarrow{CD}=\overrightarrow{CD}\times\overrightarrow{DE}=\overrightarrow{DE}\times\overrightarrow{EA}=\overrightarrow{EA}\times\overrightarrow{AB}\ne\mathbf{0} $$

$$ \overrightarrow{AB}\cdot\overrightarrow{BC}=\overrightarrow{BC}\cdot\overrightarrow{CD}=\overrightarrow{CD}\cdot\overrightarrow{DE}=\overrightarrow{DE}\cdot\overrightarrow{EA}=\overrightarrow{EA}\cdot\overrightarrow{AB} $$

1. $\overrightarrow{CE}$는 $\overrightarrow{AB}$의 상수배임을 보여라.
2. $\overrightarrow{CE}=k\overrightarrow{AB}$가 되도록 하는 상수 $k$의 가능한 값을 모두 구하고, 각각의 경우에 $ABCDE$는 어떤 도형인지 설명하여라.

#### 4.

양의 정수 $n$과 양수 $a_{ij}$ $(1\le i,j\le n)$에 대하여 $A_n,B_n,C_n$을 다음과 같이 정의하자.

$$ A_n=\left(\sum_{i=1}^n\sum_{j=1}^n a_{ij}\right)^2 $$

$$ B_n=\sum_{i=1}^n\sum_{j=1}^n a_{ij}\left(\sum_{k=1}^n a_{ik}\right)^{1/2}\left(\sum_{k=1}^n a_{kj}\right)^{1/2} $$

$$ C_n=\sum_{i=1}^n\sum_{j=1}^n a_{ij}^2 $$

다음 부등식을 증명하여라.

$$ A_n\le nB_n\le n^2C_n $$

#### 5.

양의 정수 $n$과 실수 $\alpha=\frac\pi{n+1}$에 대하여 크기가 $n\times n$인 행렬 $A$를 다음과 같이 정의하자.

$$ A=(\sin(ij\alpha))_{1\le i,j\le n} $$

행렬식 $\det A$의 절댓값을 구하여라.

#### 6.

크기가 $(n+1)\times(n+1)$인 실행렬 $A=(a_{jk})$와 $n$변수 다항식을 성분으로 가지는 크기가 $n\times n$인 행렬 $B=(b_{jk})$를 다음과 같이 정의하자.

$$ a_{jk}=j^{(n+1-k)^2}, \qquad b_{jk}=b_{jk}(x_1,\ldots,x_n)=x_j^{(n+1-k)^2-1} $$

다음을 증명하여라.

$$ \det A=(-1)^n(n!)^2\int_n^{n+1}\cdots\int_2^3\int_1^2\det B\,dx_1\,dx_2\cdots dx_n $$

#### 7.

양의 정수 $n,k\ge2$에 대하여 함수 $F:(0,1)^n\to\mathbb{R}$를 다음과 같이 정의하자.

$$ F(x_1,\ldots,x_n)=\int_{-\infty}^{\infty}\frac{1}{|t|^k+|t-x_1|^k+\cdots+|t-x_n|^k}\,dt $$

다음 적분이 수렴하는 양수 $p$의 범위를 구하여라.

$$ \int_0^1\cdots\int_0^1F(x_1,\ldots,x_n)^p\,dx_1\cdots dx_n $$
